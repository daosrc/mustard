## 本次更新

- **网页翻译按标签页独立**：只作用于开启它的那个标签页（含该标签页内跳转 / 刷新）；从当前页新开的标签或切换到别的标签页都不会被翻译，各标签页开关互不影响。

### Changes

- Page translation is now scoped to the tab that enabled it (in-tab navigation and reload included). Newly opened tabs and other tabs are no longer translated, and each tab's toggle is independent.
